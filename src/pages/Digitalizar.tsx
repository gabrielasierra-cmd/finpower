import { Link } from "react-router-dom";

export default function Digitalizar() {
  return (
    <main className="container">
      <div className="page-header">
        <nav className="page-nav">
          <ul>
            
          </ul>
        </nav>
        <h1>Digitalizar</h1>
      </div>

      <p>Utilize esta página para digitalizar recibos (simulado).</p>
      <div className="card">Botão de digitalização (simulado)</div>
    </main>
  );
}
